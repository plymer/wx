{
  "name": "client",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "tsc --noEmit && oxfmt && oxlint --fix",
    "lint": "oxlint --fix",
    "format": "oxfmt",
    "build": "vite build",
    "preview": "vite preview"
  }
}
