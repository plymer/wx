{
  "name": "client",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "tsc --noEmit && pnpm format && pnpm lint",
    "lint": "eslint . --ext .ts,.tsx --fix",
    "format": "oxfmt",
    "build": "vite build",
    "preview": "vite preview"
  }
}
