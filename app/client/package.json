{
  "name": "client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "tsc --noEmit && prettier --write . && eslint . --ext .ts,.tsx --fix",
    "lint": "eslint . --ext .ts,.tsx --fix",
    "format": "prettier --write .",
    "build": "vite build",
    "preview": "vite preview"
  },
  "overrides": {
    "vite": "npm:rolldown-vite@latest"
  }
}
